name = "ser-vital"
pages_build_output_dir = "./public"
compatibility_date = "2026-08-26"

# O binding D1 `DB` deve ser criado no Cloudflare Pages para stage e produção.
# Variável/secret esperado: ADMIN_TOKEN.
# Recomenda-se Cloudflare Access protegendo /admin/* em produção.
